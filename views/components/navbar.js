import React from 'react';
import ReactDOM from 'react-dom';

class Header extends React.Component {
	render(){
		return <h1>This currently does nothing at the moment. Will need to convert the current JS to React.JS</h1>;
	}
}

ReactDOM.render(
	test,
	document.getElementById('root')
);
